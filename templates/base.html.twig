<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="{{ asset('uploads/images/FAVICON.png') }}" type="image/x-icon">
        <link rel="stylesheet" href="{{ asset('assets/css/bootstrap.css')}}">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>


        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}

        <!-- Image positionnée en haut à gauche -->
        <img src="{{ asset('uploads/images/LOGO.png') }}" 
         alt="Logo" 
         style="position: absolute; top: 10px; left: 10px; width: 100px; z-index: 1000;">
    </head>
    </head>
    <body>
        <div class="example-wrapper"> 
                
        {% block nav %}
        {% include 'layouts/nav.html.twig'%}
        {% endblock %}
        {% block body %}
            
        {% endblock %}
         <footer class="custom-footer mt-auto py-4 text-center text-white">
            <div class="container text-center">
                <h5 class="mb-3">Knowledgelearning</h5>
                <p><strong>Adresse :</strong> Piccadilly Circus, London W1J 0DA, Royaume-Uni</p>
                <p><strong>Email :</strong> <a href="mailto:knowledgelearning@blabla.com" class="text-white text-decoration-underline">stubborn@blabla.com</a></p>
                <p class="fst-italic">"Don't compromise on your look"</p>
                <hr class="bg-white">
                <p class="mb-0">&copy; {{ "now"|date("Y") }} Knowledgelearning. Tous droits réservés.</p>
            </div>
        </footer>
    </body>
</html>
