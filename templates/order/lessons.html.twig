{% extends 'base.html.twig' %}

{% block title %}Mes cours{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4">Mes cours</h1>

        {% if courses is empty %}
            <div class="alert alert-info">Vous n'avez encore acheté aucun cours.</div>
        {% else %}
            <ul>
                {% for course in courses %}
                    <li>{{ course.title }}</li>
                {% endfor %}
            </ul>
        {% endif %}
    </div>
{% endblock %}
